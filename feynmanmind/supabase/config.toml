project_id = "feynmanmind"

# The app has no accounts, so the AI functions are public: skip JWT checks.
# They store nothing and are rate-limited per IP (see _shared/rateLimit.ts).
[functions.feynman-evaluate]
verify_jwt = false

[functions.generate-flashcards]
verify_jwt = false

[functions.generate-course]
verify_jwt = false
