# Optional: only needed for the AI features (tutor feedback, card generation).
# Without these the app still works fully offline.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
